/* ============================================================
   AIRPORT PAGE — Interactions
   2026-04-14: Filter tabs removed — 6 routes (ICN/GMP/PUS/CJU +
   Busan/Incheon Ports) are now all visible on the hub page.
   ============================================================ */
(function () {
  'use strict';
  // Airport-specific JS can go here.
  // The legacy .ap-filter-tab filter was removed when the
  // destination list grew to 6 destinations.
})();
